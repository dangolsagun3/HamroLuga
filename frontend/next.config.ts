import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.imgur.com', 'api.escuelajs.co', 'placeimg.com', 'placehold.co', 'picsum.photos', 'www.shutterstock.com']
  }
};

export default nextConfig;
